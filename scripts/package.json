{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "sync-export-record": "tsx ./src/sync-export-record.ts",
    "mix-video-audio": "tsx ./src/mix-video-audio.ts",
    "check-metadata-sync": "tsx ./src/check-metadata-sync.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.21.0",
    "@workspace/db": "workspace:*",
    "drizzle-orm": "catalog:"
  }
}
